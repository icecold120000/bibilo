{% extends 'base.html.twig' %}

{% block title %}Livre{% endblock %}

{% block body %}
    <h1>Livre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ livre.id }}</td>
            </tr>
            <tr>
                <th>TitreLivre</th>
                <td>{{ livre.titreLivre }}</td>
            </tr>
            <tr>
                <th>GenreLivre</th>
                <td>{{ livre.genreLivre }}</td>
            </tr>
            <tr>
                <th>DateLivre</th>
                <td>{{ livre.dateLivre ? livre.dateLivre|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>AfficheLivre</th>
                <td>{{ livre.afficheLivre }}</td>
            </tr>
            <tr>
                <th>NoteLivre</th>
                <td>{{ livre.noteLivre }}</td>
            </tr>
            <tr>
                <th>CommentaireLivre</th>
                <td>{{ livre.commentaireLivre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('livre_index') }}">back to list</a>

    <a href="{{ path('livre_edit', {'id': livre.id}) }}">edit</a>

    {{ include('livre/_delete_form.html.twig') }}
{% endblock %}
