{% extends 'base.html.twig' %}

{% block title %}Editer un Livre{% endblock %}

{% block body %}


    {{ include('livre/_delete_form.html.twig') }}
    <main>
        <h1>Editer un livre</h1>

        {% for message in app.flashes('SuccessBiblio') %}
            <div class="flash-success">
                {{ message }}
            </div>
        {% endfor %}

        <div>
            <div class="container-fluid" id="biblioForm">
                {{ include('livre/_form.html.twig') }}
            </div>
        </div>
        <div class="row">
            <div id="divAjout">
                <button type="submit" class="btn btn-success" form="formBibilio">
                    Modifier le livre
                </button>
                <a href="{{ path('livre_index') }}"
                   class="btn btn-secondary" id="btnRetour">
                    Revenir à la liste
                </a>
            </div>
        </div>
    </main>
{% endblock %}
